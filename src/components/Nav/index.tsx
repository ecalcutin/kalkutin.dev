import React from "react";

import styles from "./styles.less";

const Nav = (props: any) => {
  return (
    <header className={styles.header}>
      <nav></nav>
    </header>
  );
};

export default Nav;
