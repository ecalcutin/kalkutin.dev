import React, { Fragment } from "react";

import Hero from "components/sections/Hero";

const HomePage = () => {
  return (
    <Fragment>
      <Hero />
    </Fragment>
  );
};

export default HomePage;
